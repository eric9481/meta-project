import React from "react";

const ConfirmedBooking = () => {
    return (
        <section className="cb container">
            <h1 className="cb-title">Booking Confirmed!</h1>
            <p>Your reservation has been successfully made.</p>
        </section>
    );
};

export default ConfirmedBooking;